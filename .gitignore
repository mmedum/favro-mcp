# Binaries
/bin/
/dist/
/build/
*.exe
*.test
*.out
*.prof

# Coverage
coverage.out
coverage.html

# Plugin packaging
*.plugin

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Local-only config (never commit secrets)
.env
.env.local
# Dev-only MCP wiring: points at bin/favro-mcp, which is a build output.
# The shipped config for consumers is plugin-template/.mcp.json.
.mcp.json
.mcp.local.json

# Local agent / tooling state
.claude/
.entire/

# Go module cache (handled by setup-go in CI)
go.work
go.work.sum
