# How to render a code fence inside a code fence

Use a longer fence on the outside:

````markdown
Inside, you can write a regular fenced block:

```
echo "find this literal"
```

And the outer fence keeps everything intact.
````

Trailing paragraph after the fence.
