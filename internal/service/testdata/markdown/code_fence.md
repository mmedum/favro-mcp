# Card with a code fence

Some intro paragraph that mentions `inline code` and a literal `find this`.

```go
package main

func main() {
	// find this should NOT be replaced inside the fence on a literal pass
	println("find this")
}
```

Closing paragraph below the fence.
